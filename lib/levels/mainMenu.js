define(

	[
		"modules/Class"
	],

	function (Class){

		"use strict";


		return Class.extend({

			/* Creates a new canvas, adding it to the manager. */
			init: function(opts){
				this.opts = opts || {};

				var c,
					name = opts.name ||
			}

		});

});